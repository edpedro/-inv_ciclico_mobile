import {
  Center,
  Box,
  Heading,
  VStack,
  FormControl,
  Button,
  Text,
} from "native-base";

export default function LoginNative({ navigation }) {
  return <Center w="100%" bg="green.500"></Center>;
}
